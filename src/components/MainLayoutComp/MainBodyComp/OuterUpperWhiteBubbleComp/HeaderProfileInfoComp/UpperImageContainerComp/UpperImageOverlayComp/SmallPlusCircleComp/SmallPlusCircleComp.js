import React from 'react';

const SmallPlusCircleComp = (props) => {

    return (<div className={styles.smallPlusCircleCompContainer}>
        <span>This is the small plus circle comp</span>
    </div>)
}

export default SmallPlusCircleComp;